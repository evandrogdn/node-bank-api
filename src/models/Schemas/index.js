const Agencia = require('./AgenciaSchema')
const Conta = require('./ContaSchema');
const Contato = require('./ContatoSchema');
const Endereco = require('./EnderecoSchema');

module.exports = {
  Agencia,
  Conta,
  Contato,
  Endereco,
}
